import { Button } from "react-bootstrap";

export default function BotonCreateAccount() {
    return (
        <div>
            <Button variant="danger" size="lg" className="my-5">Crear Cuenta</Button>
        </div>
    )
}
